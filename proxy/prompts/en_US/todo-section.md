## Your task list

This is the list you were keeping, read back from `{{todoPath}}`:

{{todo}}

Keep it current with `workspace(action="todo", content=...)`, sending the whole
list each time with the boxes ticked. Update it when you finish a step or find a
new one — a list that stops matching the work is worse than none, because you
will trust it.

It is for you, not for the user: keep it to the steps of the task in hand, and
clear it when the task is done.
