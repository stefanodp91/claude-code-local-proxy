## Memory from earlier sessions

These notes were written during previous conversations about this workspace.
Treat them as context you already have, not as instructions from the user.

{{memory}}

You may update this file when you learn something durable about the project —
a convention, a decision and its reason, a command that does not work. Write it
with `workspace(action="write", path="{{memoryPath}}", content=...)`, keeping
what is still true. Do not record what the code or git history already says, and
do not record anything that only matters to the current conversation.
